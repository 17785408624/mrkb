<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mrkb.dao.dao.BizUserDao">
    <!-- 映射文件 -->
    <!-- 新增 -->
    <insert id="save" parameterType="com.mrkb.dao.modle.BizUser" >
			INSERT INTO users(name,age,birthday)
			values(#{name},#{age},now())
		</insert>
    <!-- 删除 -->
    <delete id="delete" parameterType="java.util.Map">
			delete from users where name = #{name}
		</delete>
    <!-- 修改 -->
    <update id="update" parameterType="java.util.Map">
			update users set name = #{name} where name = #{parameter}
		</update>

    <select id="getObject" resultType="java.util.Map">
			select * from biz_user
		</select>
</mapper>
